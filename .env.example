# Backend Configuration for Semaphore Group API

# Private key for the backend wallet that will send transactions
# This wallet should have ETH for gas and permissions to add members to the Semaphore group
BACKEND_PRIVATE_KEY=0x...

# Optional: RPC URL if you want to use a specific provider
# If not set, will use default public RPC
RPC_URL=https://sepolia.base.org

# Optional: Etherscan API key for transaction verification
ETHERSCAN_API_KEY=your_api_key_here